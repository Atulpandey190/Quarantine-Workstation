import React from "react";
import { useSelector } from "react-redux";
// Get All Users in Current Room

const RoommembersItem = (roomId) => {
  const groupCallStates = useSelector((state) => state.groupCallReducer);
  return;
};

export default RoommembersItem;
